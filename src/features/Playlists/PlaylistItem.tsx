import { PlaylistComponent, PlaylistComponentProps } from "components"

export const PlaylistItem = (props: PlaylistComponentProps) => {
  // Handle some stuff here
  return <PlaylistComponent {...props} />
}